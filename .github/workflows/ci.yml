name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      # Testcontainers needs a Docker daemon; ubuntu-latest runners already have one.
      - name: Build and test
        run: ./mvnw -B verify
        env:
          JWT_SECRET: ci-only-secret-not-used-for-anything-real
